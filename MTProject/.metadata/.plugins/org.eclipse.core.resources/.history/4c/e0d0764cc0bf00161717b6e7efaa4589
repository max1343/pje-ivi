package event;

import mygeom.Point2;
import widget.MTComponent;

public class GestureAnalyzer {

	public void analyze(MTComponent mtc, BlobQueue bq, String state, String id, Point2 p){
		if(bq.checkId(id)){
			
		}
	}
}
