package event;

import mygeom.Point2;
import widget.MTComponent;

public class GestureAnalyzer {

	public void analyze(MTComponent mtc, BlobQueue bq, String state, int id, Point2 p){
		if(bq.checkId(id)){
			//je sais pas ce qu'il faut faire si t'as une idée
		}
	}
}
